---
title: "Project4"
name: Camille Cavicchio, Jared Steinberg, and Alain Perez
output: html_document
---

```{r, message=FALSE, echo=FALSE, warning=FALSE}
library(dbplyr)
library(mdsr)
library(RMySQL)
library(ggthemes)
library(USAboundaries)
library(sf)
library(maps)
library(mapproj)
library(ggplot2)
library(RColorBrewer)
```

```{r, message=FALSE, echo=FALSE, warning=FALSE}
db<-dbConnect_scidb("fec")
```

```{r, message=FALSE, echo=FALSE, warning=FALSE, eval=FALSE}
dbListTables(db)
```
```{sql connection=db}
select * from candidates
limit 1,10
```

```{sql connection= db}
SELECT
  c.cand_id,
  c.cand_name,
  c.cand_party_affiliation,
  c.cand_election_yr,
  c.cand_office_state,
  c.cand_zip,
  b.transaction_dt, 
  sum(transaction_amt) AS total_trans
FROM candidates AS c 
LEFT JOIN contrib_com_to_cand AS b ON c.cand_id = b.cand_id 
WHERE c.cand_election_yr >= 2016 AND c.cand_id<=1 AND b.transaction_dt<=1
GROUP BY c.cand_id
order by c.cand_office_state
```

```{r, message=FALSE, echo=FALSE, warning=FALSE}
fec<-"SELECT
  c.cand_id,
  c.cand_name,
  c.cand_party_affiliation,
  c.cand_election_yr,
  c.cand_office_state,
  c.cand_zip,
  b.transaction_dt, 
  sum(transaction_amt) AS total_trans
FROM candidates AS c 
LEFT JOIN contrib_com_to_cand AS b ON c.cand_id = b.cand_id 
WHERE c.cand_election_yr >= 2016 AND c.cand_id<=1 AND b.transaction_dt<=1
GROUP BY c.cand_id
order by c.cand_office_state"
```


```{r, message=FALSE, echo=FALSE, warning=FALSE}
fecinfo<-db%>% dbGetQuery(fec) %>% collect()
fecinfo
```

```{r, message=FALSE, echo=FALSE, warning=FALSE}
pop<-read.csv("poop.csv")

pop <- pop %>%
  select(State, X2016, region) %>%
  rename(pop = "X2016") %>%
  mutate(pop = parse_number(pop))
pop

```

```{r}

fecinfo <- fecinfo %>%rename(State = 'cand_office_state',
                             ID='cand_id',
                             name='cand_name',
                             party='cand_party_affiliation',
                             year= 'cand_election_yr',
                             order= 'cand_zip',
                             transactions='total_trans') %>%
  select(-transaction_dt) %>%
  left_join(pop, by = "State")


fecinfo
```

```{r}
summed_transactions <- aggregate(transactions ~ State, fecinfo, sum)

fecinfo <- fecinfo %>%
  left_join(summed_transactions, by = "State") %>%
  select(-transactions.x) %>%
  rename(transactions = 'transactions.y') %>%
  mutate(proportion = (transactions / pop)) %>%
  mutate(new_proportion = log(proportion)) %>%
  arrange(desc(proportion))

fecinfo

pop<-read.csv("poop.csv")

```


```{r}
library(ggplot2)
library(ggmap)
library(maps)
library(mapdata)

usa <- map_data("usa") 
ggplot() + geom_polygon(data = usa, aes(x=long, y = lat, group = group)) + 
  coord_fixed(1.3)
```

```{r}
plot<-ggplot(data = states) + 
  geom_polygon(aes(x = long, y = lat, fill = region, group = group), color = "black") + 
  coord_fixed(1.3) +
  guides(fill=FALSE)
plot

us <- ggplot(data = states, mapping = aes(x = long, y = lat, group = group)) + 
  coord_fixed(1.3) + 
  geom_polygon(color = "black", fill = "NA")
us
View(states)
```

```{r}
nv_df <- subset(states, region == "nevada")
counties <- map_data("county")
nv_county <- subset(counties, region == "nevada")
```


 
```{r}

cacopa <- inner_join(states, fecinfo, by = "region")

elbow <- us + 
      geom_polygon(data = cacopa, aes(fill = new_proportion), color = "white") +
      geom_polygon(color = "black", fill = NA) +
      #geom_polygon(color= "white", data=cacopa, aes(group=name), fill=NA)+
      theme_bw() 

elbow
```
```{r}

nv_base <- ggplot(data = nv_df, mapping = aes(x = long, y = lat, group = group)) + 
  coord_fixed(1.3) + 
  geom_polygon(color = "black", fill = "gray")

nv_base<-nv_base  + 
  geom_polygon(data = nv_county, fill = NA, color = "white") +
  geom_polygon(color = "black", fill = NA)
nv_base

fecin<-fecinfo%>% filter(State=="NV")
#View(nv_df)

cacopa2 <- inner_join(nv_df, fecin, by ="order")
#View(cacopa2)

elbow2 <- nv_base + 
      geom_polygon(data = nv_county, aes(fill = order), color = "white") +
      geom_polygon(color = "black", fill = NA) +
      theme_bw() 

elbow2

```

